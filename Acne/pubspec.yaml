name: acne
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^0.8.6+3  # อัปเดตเป็นเวอร์ชันล่าสุด
  permission_handler: ^11.2.0  # เพิ่มแพคเกจสำหรับการจัดการอนุญาต
  cupertino_icons: ^1.0.6
  url_launcher: ^6.1.5  # เพิ่มแพ็กเกจ url_launcher
  dio: ^5.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/test.png
